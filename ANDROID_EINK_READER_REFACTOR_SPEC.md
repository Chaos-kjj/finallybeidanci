# 康康背词器 Android 墨水屏阅读器重构规格书

版本：1.0  
目标设备：Bigme B7 Pro（Android 14，7 英寸电子墨水屏）  
目标产物：可侧载安装、以本地数据为主、针对电子墨水屏优化的 Android APK

## 1. 项目目标

把当前纯 HTML/CSS/JavaScript PWA 重构为一个可长期维护的 Android 本地应用。在保留现有背词、SRS、错词本、学习统计、阅读进度、笔记和内置词典能力的基础上，将阅读部分提升为接近 KOReader 使用逻辑的标准阅读器。

本项目不是要求完整复制 KOReader，而是要求具备稳定的阅读引擎、书库、目录、查词、标注、AI 辅助、可调排版、翻页键和电子墨水屏视觉模式，并为以后扩展 OCR、OPDS 或更多格式保留接口。

## 2. 不可妥协的原则

1. 学习记录、书籍、阅读进度、笔记、书签、词典、AI 缓存和设置默认只保存在设备本地。
2. 删除 Supabase 登录、注册、Realtime 和云同步依赖；不得在后台上传学习数据或书籍正文。
3. 除用户主动点击 AI 操作外，应用在飞行模式下必须可以冷启动、打开书库、阅读、查本地词典、修改设置和保存进度。
4. DeepSeek API Key 不得明文保存在 `localStorage`、日志、备份文件或错误信息中，应通过 Android Keystore 支持的原生安全存储保存。
5. 所有运行时依赖必须随 APK 打包。不得从 CDN 动态加载 PDF、EPUB、词典或 UI 脚本。
6. 不得为了重构而丢失现有背词功能。先建立回归测试，再迁移代码。
7. 电子墨水屏优先于动画效果：默认无动画、无毛玻璃、无大面积阴影、无平滑滚动、无频繁渐变重绘。
8. 导入的 EPUB、HTML 词典内容一律视为不可信内容：禁止脚本执行，阻止任意网络加载，并清洗危险 HTML、URL 和事件属性。

## 3. 推荐技术架构

### 3.1 Android 容器

采用 Capacitor Android 作为主要容器，把前端资源编译进 APK。网页应运行在稳定的本地 HTTPS 风格 origin 下，不使用 `file://` 直接加载资源。

原生层负责：

- Android Storage Access Framework 文件选择、导入、导出和持久化权限。
- Android Keystore 中的 API Key 存储。
- DeepSeek/OpenAI-compatible HTTP 请求，避免 WebView CORS 并防止密钥暴露给页面脚本。
- Android 离线 TextToSpeech。
- 实体翻页键、音量键和返回键拦截。
- 应用生命周期、前后台保存和沉浸式状态栏。
- 必要时提供手动全屏刷新接口；不得假定存在未公开的 Bigme SDK。

### 3.2 Web 应用重构策略

不要一次性重写全部 `app.js`。采用逐步替换：

1. 先让现有功能在 Capacitor APK 中运行并建立回归基线。
2. 新功能放入模块化目录，通过适配器接入旧代码。
3. 先拆阅读、词典、AI、存储，再考虑拆背词 UI。
4. 如果引入 Vite + TypeScript，应保留可运行的迁移路径，避免长期存在两个状态源。

建议模块边界：

```text
src/
  app/
  core/
  storage/
  library/
  reader/
    ReaderEngine.ts
    ReaderSession.ts
    engines/
      EpubEngine.ts
      PdfEngine.ts
      TextEngine.ts
    annotations/
    pagination/
  dictionary/
    DictionaryProvider.ts
    BuiltinDictionaryProvider.ts
    StarDictProvider.ts
    MdxProvider.ts
    DictionaryImportService.ts
  ai/
    AiClient.ts
    AiActionRegistry.ts
    PromptTemplateService.ts
  eink/
    EinkProfile.ts
    HardwareKeyService.ts
  settings/
android/
docs/
tests/
```

### 3.3 数据存储

- 书籍原文件：复制到应用私有文件目录，使用稳定的 book ID；不要依赖临时 `content://` URI。
- 书库元数据、进度、书签、笔记、AI 缓存：SQLite/Room 或经过验证的 Capacitor SQLite 层。
- 用户词典索引：SQLite，优先使用 FTS5 或等价索引；不得把大型词典整体加载进内存或 `localStorage`。
- 旧版背词状态：允许先保留 `localStorage`，但必须提供 schema version 和迁移服务。
- 现有 IndexedDB 书籍：提供一次性迁移逻辑或明确的导出/导入路径。
- API Key：Android Keystore 原生安全存储。
- 缓存：可删除、可重建，并与不可丢失的用户数据分开。

应用升级必须保持相同 `applicationId` 和签名。数据库升级必须有可测试的 migration，不允许破坏性重建用户数据。

## 4. 功能需求

## 4.1 书库与文件导入

必须支持：

- PDF、EPUB 为一级格式；保留 TXT、Markdown、HTML 支持。
- 通过系统文件选择器一次或多次导入。
- 导入后复制到应用私有目录，断开原文件位置后仍可阅读。
- 提取标题、作者、封面、格式、文件大小和导入时间。
- 书库列表/网格、最近阅读、按标题/作者搜索、排序和删除。
- 使用流式 hash 或原生文件 hash 检测重复，避免把同一大文件同时读入内存两次。
- 删除书籍时明确提示是否同时删除笔记和 AI 缓存。
- 对超大文件显示可取消的导入进度，导入失败不能留下半成品记录。

暂不要求 DRM EPUB/PDF。扫描版 PDF 的 OCR 不属于第一版硬性范围，但必须明确显示“本文件没有可选文本，查词和 AI 选择不可用”，不能静默失败。

## 4.2 EPUB 阅读引擎

EPUB 必须按 package、spine、TOC 和章节渲染，不能像当前实现一样仅抽取纯文本后丢失结构。

必须支持：

- EPUB 2/3 常见文件、章节、目录、封面、图片、脚注和内部链接。
- 默认禁止 EPUB 内脚本和外部网络资源。
- 可重排分页和滚动两种模式，默认电子墨水屏分页模式。
- 基于真实 viewport 的分页；禁止继续使用固定“1800 字符一页”的算法。
- 字体、字号、字重、字间距、行高、段间距、首行缩进、正文对齐、上下左右页边距。
- 用户导入字体文件（至少 TTF/OTF），可启用、禁用和删除。
- 日间、暖色、夜间和纯黑白墨水屏主题。
- 阅读方向、左右点击区、目录跳转、章节进度、全书进度。
- 文本选择、单词查词、句子选择、书签、划线、笔记、全文搜索。
- 使用稳定位置标识保存进度和标注；优先使用 CFI/章节 href + 文本锚点，而不是脆弱的 DOM 下标。

## 4.3 PDF 阅读引擎

PDF 依赖必须本地打包。可以使用打包后的 `pdfjs-dist`，但需通过统一 `ReaderEngine` 接口封装，使未来能替换为 PDFium/MuPDF 等原生实现。

PDF 必须提供两种清晰区分的模式：

### 原版面模式

- 保持原 PDF 页面布局。
- 单页、连续页、双页（横屏可选）。
- 适合页面、适合宽度、自定义缩放、页面旋转。
- 自动/手动裁剪白边、左右/上下页边距。
- 亮度、对比度、反色、灰度和黑白增强。
- 文本层选择、查词、句子 AI 操作；无文本层时给出明确提示。
- 目录、页码跳转、缩略图、全文搜索、书签、划线和笔记。
- 只渲染当前页及邻近页，及时释放不可见 Canvas，避免大文件耗尽内存。

### 文本重排模式

- 从有文本层的 PDF 提取段落，重新排版为适合 7 英寸屏幕的阅读流。
- 支持字体、字号、字间距、行间距、段间距和页边距。
- 明确提示复杂表格、多栏、公式和图片可能失真，并允许一键回到原版面模式。

必须理解：固定版 PDF 的原版面模式无法真正改变原文的字体、字间距和行间距，这些设置只应在 EPUB、文本格式和 PDF 文本重排模式中生效。UI 不得显示无效设置制造假象。

## 4.4 自定义词典导入与查词

用户应能在“词典管理”中导入、启用、停用、排序和删除本地词典。

第一版目标格式：

1. StarDict：`.ifo + .idx + .dict`，支持 `.dict.dz`，允许以 ZIP 词典包导入。
2. MDict：`.mdx`，支持可选 `.mdd` 资源文件。
3. 应用自有词典包：ZIP 中包含 manifest、词条索引和内容，作为稳定的迁移/测试格式。

功能要求：

- 保留现有内置 Collins 词典，并纳入统一 `DictionaryProvider` 接口。
- 多词典并行查找，按用户排序展示，也可合并为标签页。
- 支持大小写、Unicode 规范化、撇号规范化、常见复数和时态回退。
- 精确查找优先，可选前缀、模糊和词形还原。
- 大词典导入在后台线程/Worker 中完成，显示进度并支持取消。
- 词典索引落 SQLite/FTS，不允许每次查词扫描完整文件。
- 词条 HTML 必须清洗；禁止执行脚本、读取应用文件或自动访问外网。
- MDD 图片/音频等资源从本地沙箱读取，限制 MIME 和单项大小。
- 导入失败必须回滚索引和临时文件。
- 展示词典名称、作者、格式、词条数、文件大小、导入日期和状态。

默认查词顺序建议：用户首选词典 → 其他已启用用户词典 → 内置 Collins → 已缓存 AI 释义。用户可调整顺序。

## 4.5 长按句子与 DeepSeek AI

在 EPUB、TXT/HTML 和具有文本层的 PDF 中：

1. 长按并完成文本选择后，在选区附近显示简洁的操作条。
2. 默认操作为“查词”“AI 解释”“AI 翻译”“复制”“做笔记”。
3. 点击“AI 解释”或“AI 翻译”后才允许发送请求；默认禁止仅因长按而自动上传文本。
4. 结果在适合墨水屏的底部面板或侧面板显示，可关闭、复制、重试、保存为笔记。
5. 结果按照 selection + paragraph context + book metadata 生成缓存 key，已缓存结果离线可再次查看。

AI 设置必须支持：

- 自定义 API Base URL，默认可填写 DeepSeek 的 OpenAI-compatible endpoint。
- 自定义模型名称、API Key、temperature、max tokens、超时。
- “AI 解释”和“AI 翻译”分别配置 system prompt 与 user prompt template。
- 模板变量至少包括：`{{selection}}`、`{{sentence}}`、`{{paragraph}}`、`{{chapterTitle}}`、`{{bookTitle}}`、`{{sourceLanguage}}`、`{{targetLanguage}}`。
- 自定义长按操作：新增、编辑、删除、启用、停用、排序操作；每项包括显示名称、提示词模板和结果展示方式。
- 可选择长按后显示哪些按钮、默认高亮哪个操作；允许显式开启“选择后自动执行某一操作”，但默认关闭并显示费用/隐私提示。
- 测试连接按钮、明确的 HTTP/鉴权/超时错误、请求取消。
- 不在日志、崩溃报告、备份中输出 API Key 或 Authorization header。
- 默认不开逐 token 刷新；若实现流式输出，应合并更新到不低于 700ms 的批次，减少墨水屏闪烁。

原生 AI 网络桥应只接受 `https://`。如用户需要局域网 HTTP 服务，必须在高级设置中显式允许，并提示明文传输风险。

## 4.6 阅读设置

设置分为全局默认和单书覆盖。单书设置可以重置为全局默认。

必须包括：

- 字体、字号、字重。
- 字间距、行间距、段间距、首行缩进。
- 左、右、上、下页边距。
- 文本对齐和断词。
- 分页/滚动、阅读方向、点击区域。
- 主题、前景色、背景色、图片灰度/对比度。
- 屏幕常亮、自动旋转、全屏/沉浸模式。
- 实体按键映射：上一页、下一页、返回、打开菜单，可交换方向。
- 每 N 页建议全刷一次的设置和手动刷新按钮；无厂商 API 时使用安全降级，不得伪称调用了硬件刷新模式。

每次设置改变都应即时预览，但电子墨水屏模式中避免连续 range slider 导致每个像素变化都重排；应节流预览并在松手后提交最终布局。

## 4.7 标准阅读器能力

第一版应具备：

- 目录、页码/章节跳转、阅读历史。
- 书签、划线、笔记及按书查看。
- 全文搜索和结果跳转。
- 最近阅读位置可靠恢复。
- 阅读时长统计。
- 单击屏幕中央开关菜单，左右区域翻页；触摸区域可配置。
- Android 返回键按“关闭弹层 → 关闭阅读菜单 → 返回书库 → 退出确认”的顺序处理。
- 意外切后台、被系统回收或强制关闭后，最近进度最多丢失一次翻页。

不作为第一版硬要求：DRM、OCR、手写批注、云同步、OPDS、局域网传书、本地大模型。架构不得阻止以后添加这些功能。

## 5. 电子墨水屏专项要求

### 5.1 视觉

- 默认纯白背景、近黑正文、实线边框，保证 300 PPI 黑白模式下清晰。
- 提供“墨水屏极速/均衡/质量”三个 UI 配置档，但如果无法控制设备 waveform，只调整应用自己的重绘策略并如实命名。
- 去除 `backdrop-filter`、透明叠层、大面积阴影、渐变、闪烁 loading、骨架动画。
- `transition` 和 `animation` 在墨水屏 profile 下一律关闭，加载状态用静态文字或离散帧。
- 禁止 `scroll-behavior: smooth`；翻页不做滑动动画。
- 高亮不能只依赖浅色颜色差，应使用下划线、边框、纹理或高对比灰阶。
- 操作区域不小于约 44dp，避免密集小图标。

### 5.2 重绘与性能

- 翻页只更新正文、页码和必要状态，不重建整个应用 shell。
- PDF 只保留有限页 Canvas；EPUB 只保留当前章节/相邻章节。
- 设置面板关闭时不参与布局和绘制。
- AI 返回默认一次性呈现，避免逐字刷新。
- 进度条只在翻页结束后更新一次。
- 大词典解析、全文索引、PDF 文本抽取在 Worker/后台线程执行。
- 页面内容提交后再保存进度，保存操作不得阻塞翻页。

### 5.3 实体按键

- 原生 Activity 拦截常见 `KEYCODE_VOLUME_UP/DOWN`、Page Up/Down 等事件。
- 默认音量下/右侧键下一页，音量上/左侧键上一页，可交换和禁用。
- 阅读界面拦截后不改变系统音量；非阅读界面保持系统默认行为。
- 若 Bigme 实际按键码不同，应提供调试页显示最近 key code，便于真机映射。

## 6. 离线与隐私边界

允许的网络请求只有用户主动触发的自定义 AI 请求。可选的应用更新检查也必须默认关闭。

必须提供一个开发/测试模式，可以记录请求域名但自动遮蔽 query、body、API Key 和 Authorization。验收时在飞行模式和阻断网络环境下测试核心功能。

备份文件默认包含：背词状态、学习设置、统计、书库元数据、书签、笔记、阅读进度、AI action 模板和普通设置。默认不包含：API Key、原书文件、可重建词典索引、AI 响应缓存。用户可选择把书籍和词典原文件打进完整 ZIP 备份，并看到预计大小。

## 7. 数据迁移

必须识别当前项目的以下数据：

- `kangkangWordPwa_state_v1`
- `mySmartWordBook_aiConfig`
- `kangkangWordPwa_definitionCache_v1`
- `kangkangWordPwa_readerSettings_v1`
- `kangkangWordPwa_readerProgress_v1`
- IndexedDB `kangkangWordPwa_readerDb/books`

迁移规则：

1. 首次启动检测旧 schema。
2. 迁移前创建本地快照，迁移事务失败时回滚。
3. API Key 不进入普通数据库；若旧配置中存在明文 Key，成功写入 Keystore 后删除明文副本。
4. 每个 migration 都有从旧 fixture 到新 schema 的自动化测试。
5. 不得自动清除用户旧数据，除非迁移成功且校验数量一致。

## 8. 性能目标

这些是 Bigme B7 Pro Release APK 的目标，实际结果必须注明是否经过真机验证：

- 本地词典精确查词：索引准备完成后通常低于 150ms。
- EPUB 普通翻页：内容切换目标低于 250ms，不含电子墨水屏硬件刷新时间。
- 书库操作不因 100 本书明显卡顿。
- 导入 100MB 文件时不把文件重复完整复制到 JavaScript 内存。
- 打开大 PDF 时内存有上限策略，不预渲染全书。
- 前后台切换和杀进程恢复后，阅读位置误差不超过一页。

## 9. 测试与验收

### 9.1 自动测试

- 保留并通过现有 `npm test`。
- 添加状态迁移、提示词模板、AI action 配置、词形归一化、词典排序和缓存 key 的单元测试。
- 使用小型合法测试 fixture 覆盖 EPUB、带文本 PDF、无文本 PDF、StarDict ZIP、MDX/MDD 或可替代的公开测试样本。
- 对 AI 使用 mock server；测试成功、401、429、500、超时、取消和畸形响应。
- 测试 EPUB/词典 HTML 清洗和禁止脚本执行。
- 测试数据库 migration、导入中断回滚和备份恢复。
- 执行 Android `assembleDebug`；如配置 release signing，再执行 release 构建。

### 9.2 离线验收

在从未联网启动过的新安装中，打开飞行模式后确认：

- 应用可以启动，无登录页面和云端错误。
- 内置完整词库可用。
- 内置 Collins 词典可查。
- 已导入 EPUB/PDF 可打开、翻页、查词、做笔记并保存进度。
- 字体和排版设置有效。
- 重启应用后书库、设置和进度仍在。
- AI 操作明确提示离线，而不是让阅读界面卡死。

### 9.3 真机验收

如果当前环境连接了 Bigme B7 Pro，应通过 `adb` 安装和测试。若没有真机，不能声称完成真机优化；必须交付一份逐项真机检查表，包括：

- CSS 像素尺寸和安全区域。
- 实体键 key code。
- 长按选择手柄和操作条位置。
- EPUB/PDF 翻页残影。
- 大 PDF 内存。
- 系统离线英文 TTS。
- 横竖屏和沉浸模式。
- 熄屏/亮屏、杀进程和升级安装后的数据恢复。

## 10. 分阶段实施顺序

1. **基线与保护**：审查工作区、运行现有测试、记录功能清单、补回归测试。
2. **Android 壳与完全本地启动**：Capacitor、固定 application ID、资源打包、移除 Supabase/UI/CDN。
3. **存储与书库**：应用私有文件、数据库 schema、文件导入、备份恢复、旧数据迁移。
4. **EPUB 引擎**：结构化渲染、目录、真实分页、排版、选择和标注。
5. **PDF 引擎**：原版面、文本层、重排、裁边、搜索和内存控制。
6. **词典系统**：统一 provider、StarDict、MDX/MDD、管理、索引和查词 UI。
7. **DeepSeek AI**：安全配置、可编辑 action/prompt、选择菜单、缓存和错误处理。
8. **电子墨水屏专项**：主题、禁动画、重绘节流、实体键、真机调试页。
9. **回归、打包与文档**：自动测试、APK、许可清单、安装升级和真机验收说明。

每一阶段都必须保持项目可运行，不得先大面积删除旧实现再长期处于不可用状态。

## 11. 完成定义

只有同时满足以下条件才能称为完成：

- 现有背词主要流程无功能回退。
- Android Debug APK 构建成功并给出明确输出路径。
- 新安装在飞行模式下可以完成核心阅读与本地查词。
- PDF、EPUB、自定义词典、DeepSeek 长按 action、全部排版设置均达到本文验收要求。
- 无 Supabase、远程词库、CDN 运行依赖；代码搜索未发现遗漏的强制远程 URL。
- API Key 不在网页存储、日志或备份中。
- 自动测试通过；无法自动化或无法真机验证的项目逐项列出，不能模糊写“应该可以”。
- 提供 `README` 中的开发、构建、侧载、升级、备份恢复和已知限制说明。
- 提供第三方依赖和许可证清单；不得把无再分发权的商业词典样本放入发布 APK 或测试仓库。

## 12. 明确禁止的捷径

- 只把当前线上 URL 放进 WebView/TWA，然后称为本地 Android 版。
- 依赖用户“先联网点过一次”才能离线查词。
- 用固定字符数模拟真实分页。
- 对 PDF 原版面显示实际无效的字体/行距设置。
- 把整个大型 PDF、EPUB 或 MDX 同时复制成多个 ArrayBuffer。
- 把 DeepSeek API Key 继续存入 `localStorage`。
- 为了赶进度删除旧背词、笔记或统计功能。
- 未运行测试或未安装真机就宣称所有功能完成。


// The runtime entry point is src/main.js, bundled by Vite for both the PWA and
// Capacitor Android. This compatibility file is intentionally side-effect free
// so older exported bookmarks do not start a second application state source.

# Bigme B7 Pro 真机验收清单

状态：软件自动化和浏览器路径已完成；当前环境没有可用的 Bigme adb 设备，以下设备相关项目不能标记为已通过。

## 已由软件验收覆盖

- [x] 新装初始化完整本地词库；飞行模式下启动、导书、全文搜索、阅读设置、书签/批注、刷新冷启动、首页月度打卡和内置词典查询。
- [x] TXT/Markdown/HTML 完整内容与段落锚点；复杂 EPUB 的图片、封面、脚注/内部链接和嵌套目录；多页 PDF 的懒加载、搜索页定位、重排/原版面路径。
- [x] data/complete 备份往返、冲突不丢已有书籍文件、损坏备份回滚、字典文件关联、AI 缓存隔离、迁移和真实活跃计时。
- [x] StarDict 64-bit idx、`sametypesequence`、`.dict.dz` 一次解压复用；MDX/MDD 声明子集和应用 ZIP 转换流程；取消/回滚/启停/排序/删除。
- [x] fast/balanced/quality 调度参数差异；默认/持久化按键映射、捕获写入 UI、阅读页拦截边界和 Back 分层的软件路径。

## 安装和升级

- [ ] Android 14 上安装 `app-debug.apk`，applicationId 为 `com.kangkang.beidanci`。
- [ ] 同一 debug 签名覆盖安装，验证学习记录、书籍和设置保留。
- [ ] 冷启动和飞行模式启动成功；不依赖外网、CDN 或 Supabase。
- [ ] Android 系统文件选择器导入 TXT/EPUB/PDF/词典，退出重进后仍可用。

## 阅读体验

- [ ] 7 英寸屏幕下 EPUB 目录、章节切换、真实分页、字号/字重/字间距/行距/段距/首行缩进/四边页边距。
- [ ] EPUB 长按选择后出现查词、AI 解释、AI 翻译、复制、做笔记和自定义 action。
- [ ] PDF 原版面缩放、适宽/旋转、裁白边、对比度、灰度/反色和文字层选择。
- [ ] PDF 文本重排设置生效；扫描 PDF 明确提示无文本层，不伪造查词能力。
- [ ] 书签、笔记、搜索、阅读进度在强制退出后恢复；章节前进/后退回到各自位置。
- [ ] 翻页无滑动动画，无 smooth scroll；确认仅正文/页码更新。

## 学习、词典和隐私

- [ ] 背词、SRS、错词本、月度打卡、7/14/30/自定义统计和内置 Collins 词典回归。
- [ ] StarDict、支持范围内 MDX/MDD、应用 ZIP 词典导入、取消、失败回滚、排序、启停、精确/词形查询。
- [ ] 检查 Android Keystore 中 Key 可用；logcat、WebView storage、普通备份均无 API Key。
- [ ] 主动点击 AI action 才发请求；离线时核心阅读/背词/词典不受影响；Native AI 取消不会泄漏线程。
- [ ] 普通备份不含书籍正文、原文件、AI Key；完整备份恢复完整性和 `dictionaryId`/`role` 关联。

## 按键调试记录

在设置页点击“记录测试按键”，依次按 Bigme 实体翻页键、音量键、返回/菜单键，记录页面显示的真实 Android key code；捕获后点击“下一页/上一页/菜单/返回”写入映射，再点击“保存墨水屏设置”。当前默认映射：

- 下一页：24、93、22
- 上一页：25、92、21
- 菜单：82
- 返回：4

重点确认：阅读页只拦截已映射按键；背词、词典、设置页不因音量键触发翻页/背词动作；capture 模式只记录并可安全退出。

## 墨水屏和性能观察

- [ ] 7 英寸屏上 fast/balanced/quality 的防抖、预取、局部刷新/整屏刷新差异可见且无过度闪烁或残影。
- [ ] 状态栏/导航栏沉浸（应用已做 API 级 best-effort 隐藏）、横竖屏重排、系统旋转、KEEP_SCREEN_ON 和返回退出行为符合设备固件。
- [ ] 大 EPUB 章节切换无明显卡死；大 PDF 只保留有限页对象/文本缓存；大词典导入可取消且内存稳定。
- [ ] 记录冷启动耗时、首次打开 EPUB/PDF 耗时、翻页耗时、导入耗时和内存异常，将实测数值补回实现状态文档。
